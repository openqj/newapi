# Project Instructions

## UI Conventions

- Keep native desktop window controls. Do not add a duplicate frontend title bar.
- Primary actions and selected segmented switches use a black background with white text.
- Standard secondary buttons use a light gray background with black text.
- Use the existing `button-primary`, `button-secondary`, and `test-mode-button` classes instead of adding one-off button colors.
